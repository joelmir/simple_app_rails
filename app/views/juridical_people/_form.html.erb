<%= simple_form_for(@juridical_person) do |f| %>
  <%= f.input :cnpj, input_html: { class: 'cnpj' } %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.select(:account_id, @accounts.map{|a| [a.name ,a.id] }) %>
  <%= f.button :submit %>
<% end %>
