<%= form_for(@juridical_person) do |f| %>
  <% if @juridical_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@juridical_person.errors.count, "error") %> prohibited this juridical_person from being saved:</h2>

      <ul>
      <% @juridical_person.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cnpj %><br />
    <%= f.text_field :cnpj %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :account %><br />
    <%= f.select(:account_id, @accounts.map{|a| [a.name ,a.id] }) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
